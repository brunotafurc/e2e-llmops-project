resources:
  models:
    model:
      name: ${var.model_name}

  experiments:
    experiment:
      name: ${var.experiment_name}

  schemas:
    sentiment_schema:
      name: ${var.schema_name}
      catalog_name: ${var.catalog_name}